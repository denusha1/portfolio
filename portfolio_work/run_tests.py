"""Entry point to execute the test suite for the stringkit package."""

import sys
import pytest

if __name__ == "__main__":
    # Run pytest with default arguments; exit with the appropriate code.
    sys.exit(pytest.main())
