# Decision: Artifact storage: MinIO (S3 compatible) as configurable artifact repository

- **Kind:** decision
- **Recorded:** 2026-09-02T18:36:35

## Decision
Artifact storage: MinIO (S3 compatible) as configurable artifact repository

## Reason
Provides on-premise object storage with S3 API, aligns with requirement for configurable artifact repository and supports encryption at rest.
